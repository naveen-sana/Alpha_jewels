[phases.setup]
nixPkgs = ["mvn", "openjdk21"]

[phases.build]
cmds = ["rm -rf target ../target", "mvn clean package -DskipTests"]

[start]
cmd = "java -jar target/jewellery-backend-0.0.1-SNAPSHOT.jar"
