package com.jewellery.config;

/**
 * CORS is handled by Spring Security in SecurityConfig.
 *
 * This filter is intentionally disabled to prevent duplicate
 * or conflicting CORS headers.
 */
public class CustomCorsFilter {
    // Intentionally unused.
}